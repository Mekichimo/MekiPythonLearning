git status
git log
git log [sciezka do pliku/folderu]

git add [sciezka do pliku/folderu]
git commit -m "nazwa commitu"
git push

git rm [sciezka]
git rm -r [sciezka]

git pull --rebase - sciagasz commity z serwera do lokalnego repozytorium
